# 高校创新项目系统 - 未实装功能规划

> 本文档记录 PRD 中曾规划但当前版本尚未实装的功能，以及已知系统限制与改进方向。
> 创建日期：2026-05-05

---

## 1. 未实装功能清单

### 1.1 评审回避机制

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 自动回避项目负责人所在学院的专家，保证评审公平 |
| **当前状态** | 未实装。`POST /reviews/assignments` 分配专家时无任何回避校验 |
| **优先级** | 高 |
| **实装建议** | 在 `ExpertAssignmentServiceImpl.assignExpert()` 中增加校验：若专家为校内专家（`is_inner=1`），检查其所属学院是否与项目所属学院相同，相同则拒绝分配并提示"回避原则：不可分配与项目负责人同学院的校内专家" |

### 1.2 评审意见富文本

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 支持富文本填写评审意见 |
| **当前状态** | 未实装。`ReviewScoreDTO` 和 `project_review_score` 表仅有分数字段，无 opinion 字段 |
| **优先级** | 中 |
| **实装建议** | 1. 在 `project_review_score` 表增加 `opinion` TEXT 字段；2. 在 `ReviewScoreDTO` 增加 `opinion` 字段；3. 评审意见存储至 MongoDB `review_opinion_detail` 集合（长文本），`project_review_score` 表仅存摘要或引用 ID |

### 1.3 甘特图进度看板

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 以甘特图形式可视化展示项目整体进度，一目了然 |
| **当前状态** | 未实装。前端无甘特图组件 |
| **优先级** | 低 |
| **实装建议** | 前端引入甘特图组件库（如 `dhtmlx-gantt` 或 `vue-ganttastic`），基于 `project_milestone` 数据渲染时间轴视图 |

### 1.4 同期参与项目数限制

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 限制同一学生同期参与项目数量（最多 2 项） |
| **当前状态** | 未实装。创建项目和添加成员时无此校验 |
| **优先级** | 中 |
| **实装建议** | 在 `ProjectServiceImpl.createProject()` 和添加成员逻辑中，查询该用户当前参与的进行中项目数（`status NOT IN ('concluded','rejected')`），若 ≥ 2 则拒绝 |

### 1.5 文件格式与大小校验

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 自动校验文件格式（仅支持 PDF/Word）与大小（限制 10MB） |
| **当前状态** | 未实装。`FileController.uploadFile()` 直接上传至 MinIO，无格式/大小校验 |
| **优先级** | 中 |
| **实装建议** | 在 `AttachmentService.uploadFile()` 中增加校验逻辑：1. 检查文件后缀名（`.pdf/.doc/.docx`）；2. 检查 `file.getSize() <= 10 * 1024 * 1024`；3. 不符合则抛出异常 |

### 1.6 草稿自动保存

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 支持自动保存草稿，学生可分多次填写 |
| **当前状态** | 未实装。当前仅支持手动保存草稿 |
| **优先级** | 低 |
| **实装建议** | 前端在 `ProjectCreate.vue` 中增加定时器（如每 60 秒），自动调用 `PUT /projects/{id}` 保存当前表单内容，并显示"已自动保存"提示 |

### 1.7 数据导出 Excel

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 支持将统计数据导出为 Excel 报表，用于上报材料 |
| **当前状态** | 未实装。统计接口仅返回 JSON 数据 |
| **优先级** | 中 |
| **实装建议** | 1. 后端新增 `GET /statistics/export` 接口，使用 Apache POI（已引入）生成 Excel；2. 返回 `Content-Type: application/octet-stream` 的文件流；3. 前端使用 `<a download>` 触发下载 |

### 1.8 可视化大屏

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 全校项目总览看板，实时展示申报趋势、立项率、成果转化率 |
| **当前状态** | 未实装。前端无数据大屏页面 |
| **优先级** | 低 |
| **实装建议** | 新增 `/statistics/dashboard` 页面，使用 ECharts 图表组件，调用现有统计 API 渲染折线图、饼图、柱状图等 |

### 1.9 中期检查与结题提交

| 项 | 内容 |
|----|------|
| **PRD 原描述** | 学生提交中期检查报告、结题材料 |
| **当前状态** | 未实装。`project_mid_check` 和 `project_conclude` 表已存在，但无对应的提交/审核 API |
| **优先级** | 高 |
| **实装建议** | 1. 新增 `POST /projects/{id}/mid-check` 提交中期检查；2. 新增 `POST /projects/{id}/conclude` 提交结题申请；3. 新增管理员审核接口；4. 前端新增对应页面 |

---

## 2. 已知系统限制

| 限制项 | 说明 | 优先级 | 改进建议 |
|--------|------|--------|----------|
| 校外专家无法线上评审 | 外部专家无系统账号（`user_id` 为 NULL），无法登录系统进行评审打分 | 高 | 为外部专家创建临时登录账号，或强制 `expert` 表与 `user` 表关联 |
| 无法撤销专家分配 | 误分配专家后无删除分配的接口 | 中 | 增加 `DELETE /reviews/assignments/{id}` 接口 |
| 项目成员参与数无限制 | 当前未校验同一学生同期参与项目数量 | 中 | 同 1.4 |
| 导师审核无意见字段 | `project_teacher_audit` 表无 opinion 字段，导师审核时无法填写审核意见 | 中 | 在审核表中增加 `opinion` TEXT 字段，DTO 中同步增加 |
| 学院/学校终审无意见字段 | 同上，终审表无 opinion 字段 | 中 | 同上 |

---

## 3. 功能优先级排序

| 优先级 | 功能 | 预估工作量 |
|--------|------|-----------|
| **P0** | 中期检查与结题提交 API | 3 天 |
| **P0** | 评审回避机制 | 0.5 天 |
| **P1** | 文件格式与大小校验 | 0.5 天 |
| **P1** | 同期参与项目数限制 | 0.5 天 |
| **P1** | 评审意见富文本 | 1 天 |
| **P1** | 撤销专家分配接口 | 0.5 天 |
| **P1** | 审核/终审意见字段 | 0.5 天 |
| **P1** | 数据导出 Excel | 1 天 |
| **P1** | 校外专家线上评审 | 2 天 |
| **P2** | 草稿自动保存 | 0.5 天 |
| **P2** | 可视化大屏 | 3 天 |
| **P2** | 甘特图进度看板 | 2 天 |
