# 高校创新项目系统技术文档

本文档包含项目完整的技术规格、数据模型与接口契约，作为开发团队的统一标准。

---

## 1. 技术规格书 (Technical Specification)

### 1.1 技术栈清单 (Tech Stack)

> **强制规范**：开发必须严格遵循以下技术选型，禁止使用未列出的过时或不兼容库。

|层级|技术选型|版本要求|说明|地址|用户名|密码|
|---|---|---|---|---|---|---|
|**后端语言**|Java|11+|后端主语言|/|/|/|
|**后端框架**|Spring Boot|2.7.x|微服务基础框架|/|/|/|
|**ORM 框架**|MyBatis + MyBatis-Plus|3.5.x|数据库操作|/|/|/|
|**关系型数据库**|MySQL|8.0|核心结构化数据存储|192.168.100.128：3307|root|root|
|**文档数据库**|MongoDB|5.0+|半结构化 / 日志数据存储|192.168.100.128:27017|/|/|
|**对象存储**|MinIO|RELEASE.2023|文件存储，兼容 S3 协议|192.168.100.128:9000|minioadmin|minioadmin|
|**消息队列**|RabbitMQ|3.x|异步消息推送，消息中心|192.168.100.128:5672|guest|guest|
|**前端框架**|Vue.js|3.x|前端视图层|/|/|/|
|**前端语言**|TypeScript|4.9+|前端类型安全|/|/|/|
|**UI 组件库**|Element Plus|2.3+|UI 组件|/|/|/|
|**认证方案**|JWT (jjwt)|0.11.5|Token 身份认证，有效期 86400ms|/|/|/|
|**密码加密**|BCrypt|Spring Security 内置|密码哈希存储|/|/|/|
|**接口文档**|Knife4j (OpenAPI 3.0)|4.1.x|Swagger 增强版接口文档|/|/|/|
|**文件处理**|Apache POI|5.2.x|Excel 导入导出|/|/|/|

### 1.2 核心逻辑说明

#### 1.2.1 项目状态流转逻辑 (State Machine)

项目状态遵循严格的线性流转规则，禁止跨状态跳转。当前系统共定义 14 种项目状态：

| 状态值 | 中文含义 | 可流转至 |
|--------|----------|----------|
| `draft` | 草稿 | `wait_teacher_audit` |
| `wait_teacher_audit` | 待导师审核 | `draft`(驳回), `wait_college_assign`(通过), `rejected` |
| `wait_college_assign` | 待院级分配专家 | `wait_college_review`(分配后自动推进) |
| `wait_college_review` | 待院级评审 | `wait_college_audit`(所有专家打分后自动推进) |
| `wait_college_audit` | 待院级终审 | `wait_school_assign`(通过), `rejected`(驳回) |
| `wait_school_assign` | 待校级分配专家 | `wait_school_review`(分配后自动推进) |
| `wait_school_review` | 待校级评审 | `wait_school_audit`(所有专家打分后自动推进) |
| `wait_school_audit` | 待校级终审 | `approved`(通过), `rejected`(驳回) |
| `approved` | 已立项 | `running` |
| `rejected` | 已驳回 | `draft`(可重新提交) |
| `running` | 运行中 | `mid_checking` |
| `mid_checking` | 中期检查 | `running`, `conclude_apply` |
| `conclude_apply` | 待结题 | `concluded`, `rejected` |
| `concluded` | 已结题 | - |

```pseudocode
// 状态流转校验伪代码
function validateStateTransition(currentStatus, targetStatus, operatorRole) {
  const flow = {
    'draft': ['wait_teacher_audit'],
    'wait_teacher_audit': ['draft', 'wait_college_assign', 'rejected'],
    'wait_college_assign': ['wait_college_review'],
    'wait_college_review': ['wait_college_audit'],
    'wait_college_audit': ['wait_school_assign', 'rejected'],
    'wait_school_assign': ['wait_school_review'],
    'wait_school_review': ['wait_school_audit'],
    'wait_school_audit': ['approved', 'rejected'],
    'approved': ['running'],
    'rejected': ['draft'],
    'running': ['mid_checking'],
    'mid_checking': ['running', 'conclude_apply'],
    'conclude_apply': ['concluded', 'rejected']
  };

  // 1. 检查流转是否合法
  if (!flow[currentStatus]?.includes(targetStatus)) {
    throw new Error("非法的状态流转");
  }

  // 2. 检查操作人权限
  if (!checkPermission(operatorRole, currentStatus, targetStatus)) {
    throw new Error("权限不足，无法执行此操作");
  }

  return true;
}
```

#### 1.2.2 专家分配自动推进逻辑

管理员为项目分配专家后，系统自动将项目状态从"待分配"推进至"待评审"。

```pseudocode
function afterExpertAssigned(projectId, stage) {
  Project project = getProject(projectId);
  if (stage === 'college' && project.status === 'wait_college_assign') {
    updateProjectStatus(projectId, 'wait_college_review');
  } else if (stage === 'school' && project.status === 'wait_school_assign') {
    updateProjectStatus(projectId, 'wait_school_review');
  }
}
```

#### 1.2.3 专家评审自动推进逻辑

当某一阶段（院级 / 校级）的所有分配专家均完成打分后，系统自动将项目状态推进至下一审批环节。

```pseudocode
function afterExpertScored(projectId, stage) {
  // 查询该项目该阶段的所有分配专家
  const assignedExperts = getAssignedExperts(projectId, stage);
  // 查询已完成打分的专家
  const scoredExperts = getScoredExperts(projectId, stage);

  if (scoredExperts.length >= assignedExperts.length && assignedExperts.length > 0) {
    // 所有专家打分完毕，自动推进状态
    if (stage === 'college') {
      updateProjectStatus(projectId, 'wait_college_audit');
    } else if (stage === 'school') {
      updateProjectStatus(projectId, 'wait_school_audit');
    }
  }
}
```

#### 1.2.4 项目提交校验逻辑

学生提交项目时，系统进行以下校验：
- 仅草稿(`draft`)或被驳回(`rejected`)的项目可提交
- 仅项目负责人可提交
- **必须选择指导老师**，否则提交失败并提示"请先选择指导老师后再提交"

#### 1.2.5 里程碑预警逻辑

系统每日定时任务扫描，对即将到期或已逾期的里程碑自动标记预警。

```pseudocode
// 每日凌晨执行
function checkMilestoneWarning() {
  const today = getToday();
  const milestones = getActiveMilestones();

  for (const m of milestones) {
    if (m.status === 'finished') continue;

    const daysLeft = daysBetween(today, m.plan_time);
    
    if (daysLeft < 0) {
      // 已逾期
      updateMilestone(m.id, { status: 'overdue', is_warning: 1 });
      sendWarningNotification(m.project_id, `里程碑【${m.name}】已逾期！`);
    } else if (daysLeft <= 3) {
      // 3天内即将到期
      updateMilestone(m.id, { is_warning: 1 });
      sendWarningNotification(m.project_id, `里程碑【${m.name}】即将到期！`);
    }
  }
}
```

---

## 2. 数据库 Schema 与数据模型 (Database Schema)

本系统采用 **MySQL + MongoDB + MinIO** 混合存储架构。

### 2.1 核心实体关系图 (ERD)

![Image](https://internal-api-drive-stream.larkoffice.com/space/api/box/stream/download/authcode/?code=ZDBhNWI4Y2NjMTQ5ZWM0MzgxMWQ4NmE5NmM5OWUwYTBfYzFhN2YyOTlhY2ZmZTgwZjIwOTE0OTc2NTEyM2ExMGZfSUQ6NzYzMDQzNzY5NTA1MTA1ODM2OV8xNzc2NjYwNDM3OjE3NzY3NDY4MzdfVjM)

### 2.2 MySQL 结构化数据定义

以下为核心业务表的 DDL 定义，所有表均使用 InnoDB 引擎，字符集 utf8mb4。

#### 2.2.1 基础信息表

```sql
-- 用户表
CREATE TABLE `user` (
  `user_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '用户唯一ID',
  `username` VARCHAR(50) NOT NULL COMMENT '登录账号：学号/工号',
  `password` VARCHAR(100) NOT NULL COMMENT 'BCrypt加密密码',
  `real_name` VARCHAR(20) NOT NULL COMMENT '真实姓名',
  `phone` VARCHAR(20) COMMENT '手机号',
  `email` VARCHAR(50) COMMENT '邮箱',
  `role` ENUM('student','teacher','college_admin','school_admin','expert') NOT NULL COMMENT '系统角色',
  `college_id` INT COMMENT '所属学院ID',
  `major` VARCHAR(50) COMMENT '学生专业',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1正常 0禁用',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `idx_username` (`username`),
  KEY `idx_college` (`college_id`)
) ENGINE=InnoDB;

-- 学院表
CREATE TABLE `college` (
  `college_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '学院ID',
  `college_name` VARCHAR(50) NOT NULL COMMENT '学院名称',
  `sort` INT DEFAULT 0 COMMENT '展示排序',
  `status` TINYINT DEFAULT 1,
  UNIQUE KEY `idx_college_name` (`college_name`)
) ENGINE=InnoDB;

-- 项目类别表
CREATE TABLE `project_category` (
  `cat_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '类别ID',
  `cat_name` VARCHAR(30) NOT NULL COMMENT '类别：创新训练/创业训练/创业实践',
  `remark` VARCHAR(200) COMMENT '类别说明',
  UNIQUE KEY `idx_cat_name` (`cat_name`)
) ENGINE=InnoDB;
```

#### 2.2.2 项目申报与团队

```sql
-- 项目主表（核心）
CREATE TABLE `project` (
  `project_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '项目唯一ID',
  `project_name` VARCHAR(100) NOT NULL COMMENT '项目名称',
  `cat_id` INT NOT NULL COMMENT '项目类别ID',
  `leader_id` INT NOT NULL COMMENT '负责人ID',
  `teacher_id` INT COMMENT '指导老师ID（创建时可选，提交时必填）',
  `college_id` INT NOT NULL COMMENT '所属学院',
  `apply_year` INT NOT NULL COMMENT '申报年份（统计维度）',
  `apply_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '申报时间',
  `total_budget` DECIMAL(10,2) COMMENT '总申请经费',
  `status` ENUM(
    'draft','wait_teacher_audit','wait_college_assign','wait_college_review','wait_college_audit',
    'wait_school_assign','wait_school_review','wait_school_audit','approved','rejected',
    'running','mid_checking','conclude_apply','concluded'
  ) NOT NULL DEFAULT 'draft' COMMENT '项目状态（14种）',
  `start_time` DATE COMMENT '项目开始时间',
  `end_time` DATE COMMENT '计划结束时间',
  `version` INT NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
  KEY `idx_leader` (`leader_id`),
  KEY `idx_teacher` (`teacher_id`),
  KEY `idx_college_year` (`college_id`,`apply_year`),
  KEY `idx_status` (`status`),
  KEY `idx_apply_time` (`apply_time`)
) ENGINE=InnoDB;

-- 项目成员表
CREATE TABLE `project_member` (
  `member_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '成员ID',
  `project_id` INT NOT NULL COMMENT '项目ID',
  `user_id` INT NOT NULL COMMENT '学生用户ID',
  `role` ENUM('leader','normal') NOT NULL COMMENT '角色：负责人/成员',
  `join_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `idx_project_user` (`project_id`,`user_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB;
```

#### 2.2.3 评审与审批

```sql
-- 专家库表
CREATE TABLE `expert` (
  `expert_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '专家ID',
  `user_id` INT UNIQUE COMMENT '关联系统用户ID（校外专家可为NULL）',
  `real_name` VARCHAR(20) NOT NULL COMMENT '专家姓名',
  `unit` VARCHAR(100) NOT NULL COMMENT '工作单位',
  `title` VARCHAR(30) COMMENT '职称',
  `research_field` VARCHAR(100) COMMENT '研究方向',
  `is_inner` TINYINT DEFAULT 1 COMMENT '是否校内专家',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1可用 0禁用',
  KEY `idx_field` (`research_field`)
) ENGINE=InnoDB;

-- 专家分配表
CREATE TABLE `expert_assignment` (
  `assignment_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '分配ID',
  `project_id` INT NOT NULL COMMENT '项目ID',
  `expert_id` INT NOT NULL COMMENT '专家ID',
  `stage` ENUM('college','school') NOT NULL COMMENT '评审阶段：院级/校级',
  `assign_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '分配时间',
  `deadline` DATETIME COMMENT '评审截止时间',
  KEY `idx_project` (`project_id`),
  KEY `idx_expert` (`expert_id`)
) ENGINE=InnoDB COMMENT='专家分配表';

-- 评审打分表
CREATE TABLE `project_review_score` (
  `score_id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '评分ID',
  `project_id` INT NOT NULL COMMENT '项目ID',
  `expert_id` INT NOT NULL COMMENT '专家ID',
  `review_stage` ENUM('college','school') NOT NULL COMMENT '评审阶段：院级/校级',
  `score_innovation` TINYINT COMMENT '创新性得分',
  `score_feasibility` TINYINT COMMENT '可行性得分',
  `score_team` TINYINT COMMENT '团队得分',
  `score_value` TINYINT COMMENT '价值得分',
  `total_score` TINYINT NOT NULL COMMENT '总分',
  `score_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `idx_expert_project` (`project_id`,`expert_id`,`review_stage`),
  KEY `idx_project_stage` (`project_id`,`review_stage`)
) ENGINE=InnoDB;

-- 导师审核表
CREATE TABLE `project_teacher_audit` (
  `audit_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL UNIQUE COMMENT '项目ID',
  `teacher_id` INT NOT NULL,
  `result` ENUM('pass','reject') NOT NULL,
  `audit_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_teacher` (`teacher_id`)
) ENGINE=InnoDB;

-- 学院终审表
CREATE TABLE `project_college_audit` (
  `audit_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL UNIQUE,
  `admin_id` INT NOT NULL,
  `result` ENUM('pass','reject') NOT NULL,
  `audit_time` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- 学校终审表
CREATE TABLE `project_school_audit` (
  `audit_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL UNIQUE,
  `admin_id` INT NOT NULL,
  `result` ENUM('pass','reject') NOT NULL,
  `audit_time` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;
```

#### 2.2.4 过程管理与成果

```sql
-- 里程碑表
CREATE TABLE `project_milestone` (
  `milestone_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL,
  `milestone_name` VARCHAR(50) NOT NULL,
  `plan_time` DATE NOT NULL,
  `actual_time` DATE,
  `status` ENUM('pending','doing','finished','overdue') NOT NULL DEFAULT 'pending',
  `is_warning` TINYINT DEFAULT 0,
  KEY `idx_project` (`project_id`),
  KEY `idx_plan_time` (`plan_time`)
) ENGINE=InnoDB;

-- 中期检查表
CREATE TABLE `project_mid_check` (
  `mid_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL UNIQUE,
  `submit_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `status` ENUM('waiting','pass','reject') DEFAULT 'waiting'
) ENGINE=InnoDB;

-- 成果主表
CREATE TABLE `project_achievement` (
  `achievement_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL,
  `type` ENUM('patent','paper','software','competition','business','other') NOT NULL,
  `name` VARCHAR(100) NOT NULL,
  `achievement_no` VARCHAR(50) COMMENT '成果编号',
  `publish_time` DATE,
  `status` ENUM('applying','approved','published','landed') NOT NULL,
  KEY `idx_project` (`project_id`),
  KEY `idx_type` (`type`),
  KEY `idx_time` (`publish_time`)
) ENGINE=InnoDB;

-- 结题验收表
CREATE TABLE `project_conclude` (
  `conclude_id` INT PRIMARY KEY AUTO_INCREMENT,
  `project_id` INT NOT NULL UNIQUE,
  `submit_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `status` ENUM('waiting','pass','reject') DEFAULT 'waiting'
) ENGINE=InnoDB;

-- 附件元数据表
CREATE TABLE `attachment` (
  `attach_id` INT PRIMARY KEY AUTO_INCREMENT,
  `attach_type` ENUM('apply','mid','conclude','achievement') NOT NULL COMMENT '附件类型',
  `relation_id` INT NOT NULL COMMENT '关联ID：项目ID/成果ID',
  `file_name` VARCHAR(100) NOT NULL,
  `file_size` BIGINT NOT NULL COMMENT '文件大小(Byte)',
  `minio_path` VARCHAR(200) NOT NULL COMMENT 'MinIO存储路径',
  `upload_user` INT NOT NULL,
  `upload_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_relation` (`relation_id`,`attach_type`)
) ENGINE=InnoDB;

-- 消息中心表
CREATE TABLE `message` (
  `message_id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '消息ID',
  `receiver_id` INT NOT NULL COMMENT '接收者用户ID',
  `sender_id` INT COMMENT '发送者用户ID(系统消息为NULL)',
  `title` VARCHAR(100) NOT NULL COMMENT '消息标题',
  `content` TEXT NOT NULL COMMENT '消息内容',
  `type` ENUM('system','audit','review','milestone','achievement') NOT NULL DEFAULT 'system' COMMENT '消息类型',
  `relation_id` INT COMMENT '关联业务ID(项目ID等)',
  `is_read` TINYINT DEFAULT 0 COMMENT '是否已读: 0未读 1已读',
  `read_time` DATETIME COMMENT '已读时间',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_receiver` (`receiver_id`),
  KEY `idx_type` (`type`),
  KEY `idx_read` (`receiver_id`,`is_read`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB COMMENT='消息中心表';
```

### 2.3 消息中心架构 (Message Center)

基于 RabbitMQ 的异步消息通知系统，实现业务事件的实时推送。

#### 架构流程

```
业务操作 → MessageProducer → RabbitMQ TopicExchange → Queue → MessageConsumer → MySQL持久化 → 前端轮询(30s)
```

#### Exchange / Queue / Routing Key 映射

| Exchange | Queue | Routing Key | 消息类型 |
|----------|-------|-------------|----------|
| innovation.msg.exchange | innovation.msg.audit | msg.audit | 审核通知 |
| innovation.msg.exchange | innovation.msg.review | msg.review | 评审通知 |
| innovation.msg.exchange | innovation.msg.milestone | msg.milestone | 里程碑通知 |
| innovation.msg.exchange | innovation.msg.achievement | msg.achievement | 成果通知 |
| innovation.msg.exchange | innovation.msg.system | msg.system | 系统通知 |

#### 消息触发场景

| 触发场景 | 消息类型 | 接收者 |
|----------|----------|--------|
| 学生提交项目 | audit | 指导老师 |
| 导师审核(通过/驳回) | audit | 项目负责人 |
| 学院审核(通过/驳回) | audit | 项目负责人 |
| 学校审核(通过/驳回) | audit | 项目负责人 |
| 管理员分配专家 | review | 被分配的专家(有关联userId的) |
| 专家提交评审打分 | review | 项目负责人 |
| 新增里程碑 | milestone | 项目负责人 |
| 提交新成果 | achievement | 项目负责人 |
| 管理员发送系统通知 | system | 指定用户 |

### 2.4 MongoDB 半结构化数据定义

用于存储动态、大字段数据，无需修改表结构即可扩展。

|集合 (Collection)|说明|索引|
|---|---|---|
|`project_detail`|项目申报详细内容、富文本、动态预算|`{ project_id: 1 }` Unique|
|`review_opinion_detail`|专家长文本评审意见|`{ score_id: 1 }` Unique|
|`achievement_extend`|不同类型成果的专属扩展字段|`{ achievement_id: 1 }` Unique|
|`system_operation_log`|系统操作日志|`{ create_time: 1 }`, `{ user_id: 1, create_time: -1 }`|
|`milestone_log`|里程碑变更历史|`{ project_id: 1, create_time: -1 }`|

### 2.5 MinIO 对象存储定义

|桶 (Bucket)|用途|生命周期|
|---|---|---|
|`project-attachments`|项目申报 / 中期 / 结题附件|永久保留|
|`achievement-materials`|成果证明材料|永久保留|
|`temp-files`|临时文件、上传草稿|7 天自动清理|
|`system-backup`|系统备份|30 天轮转|

---

## 3. API 接口定义 (API Contracts)

### 3.1 通用规范

- **Base URL**: `/api/v1`
- **后端端口**: 8081
- **认证**: 所有接口（除登录、注册外）均需在 Header 中携带 `Authorization: Bearer <token>`
- **Token 有效期**: 86400ms（24小时）
- **登出机制**: Token 加入黑名单（基于 jti + 过期时间的内存黑名单）
- **用户禁用**: 禁用用户时自动将其 Token 加入黑名单

- **统一响应格式**:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

- **错误响应格式**:

```json
{
  "code": 400,
  "message": "参数校验失败：项目名称不能为空",
  "data": null
}
```

- **分页响应格式**:

```json
{
  "code": 200,
  "data": {
    "total": 128,
    "list": []
  }
}
```

### 3.2 认证管理接口 (`/api/v1/auth`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/auth/login` | 用户登录 | 公开 |
| POST | `/auth/register` | 用户注册（强制 student 角色） | 公开 |
| POST | `/auth/logout` | 用户登出（Token 加入黑名单） | 已登录 |

#### 3.2.1 用户登录

**请求体**:

```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应体**:

```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1,
    "username": "admin",
    "realName": "系统管理员",
    "role": "school_admin",
    "collegeId": null
  }
}
```

#### 3.2.2 用户注册

**请求体**:

```json
{
  "username": "2024001",
  "password": "123456",
  "realName": "张三",
  "phone": "13800138000",
  "email": "zhangsan@example.com",
  "collegeId": 1,
  "major": "计算机科学与技术"
}
```

> 注册接口强制设置 `role=student`，防止越权注册管理员账号。

### 3.3 用户管理接口 (`/api/v1/users`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| **GET** | **`/users/teachers`** | **获取导师列表** | **所有已登录用户** |
| GET | `/users/me` | 获取当前用户信息 | 已登录 |
| GET | `/users` | 获取用户列表（分页） | college_admin, school_admin |
| GET | `/users/{id}` | 获取用户详情 | 已登录 |
| POST | `/users` | 创建用户 | college_admin, school_admin |
| PUT | `/users/{id}` | 更新用户 | college_admin, school_admin |
| PUT | `/users/{id}/status` | 禁用/启用用户 | college_admin, school_admin |
| DELETE | `/users/{id}` | 删除用户 | school_admin |
| PUT | `/users/change-password` | 修改密码 | 已登录 |

#### 3.3.1 获取导师列表

**接口信息**:
- 路径: `GET /users/teachers`
- 说明：供学生创建项目时选择指导老师使用，所有已登录用户均可访问
- 查询参数: `?collegeId=1&realName=张`

**响应体**:

```json
{
  "code": 200,
  "data": [
    {
      "userId": 5,
      "realName": "张教授",
      "collegeId": 1,
      "major": "人工智能",
      "phone": "13800000001",
      "email": "zhang@example.com"
    }
  ]
}
```

> 仅返回 `role=teacher` 且 `status=1` 的用户，筛选非敏感字段（userId, realName, collegeId, major, phone, email），按 realName 升序排列。

### 3.4 项目管理接口 (`/api/v1/projects`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/projects` | 创建项目 | student, teacher, college_admin, school_admin |
| GET | `/projects` | 获取项目列表（角色权限过滤） | 已登录 |
| GET | `/projects/{id}` | 获取项目详情 | 已登录(学生仅可查看自己的项目) |
| POST | `/projects/{id}/submit` | 提交项目 | student, teacher |
| POST | `/projects/teacher-audit` | 导师审核 | teacher |
| POST | `/projects/college-audit` | 学院终审 | college_admin |
| POST | `/projects/school-audit` | 学校终审 | school_admin |
| PUT | `/projects/{id}` | 更新项目（status/leaderId/collegeId/applyYear 不可通过此接口修改） | student, teacher, college_admin, school_admin |
| DELETE | `/projects/{id}` | 删除项目 | college_admin, school_admin |

#### 3.4.1 创建项目申报

**请求体**:

```json
{
  "projectName": "基于AI的智能校园导航系统",
  "catId": 1,
  "collegeId": 5,
  "teacherId": 8,
  "totalBudget": 10000.00,
  "startTime": "2024-03-01",
  "endTime": "2025-03-01",
  "content": "这是一个基于大语言模型的校园导航...",
  "innovationPoints": ["AI交互", "室内定位", "个性化推荐"],
  "techRoute": "<p>富文本技术路线...</p>",
  "budgetDetail": {
    "device": 5000,
    "material": 3000,
    "travel": 2000
  },
  "members": [
    { "userId": 2024002, "role": "normal" },
    { "userId": 2024003, "role": "normal" }
  ]
}
```

> **重要**：`teacherId` 为必填字段，提交项目时系统校验 `teacherId != null`，否则返回错误"请先选择指导老师后再提交"。创建者自动作为 `leader` 成员加入项目。

**响应体**:

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "projectId": 1001,
    "status": "draft",
    "applyTime": "2024-04-19T12:00:00Z"
  }
}
```

#### 3.4.2 获取项目列表

**查询参数**: `?page=1&size=10&status=wait_teacher_audit&collegeId=5&applyYear=2024&projectName=AI&leaderId=10&teacherId=8`

> 权限过滤规则：
> - **student**: 只能看到自己是负责人或成员的项目
> - **teacher**: 默认看到自己指导的项目
> - **college_admin**: 默认看本院项目
> - **school_admin**: 可看所有项目

#### 3.4.3 提交项目

- 路径: `POST /projects/{id}/submit`
- 校验：仅草稿/被驳回项目可提交，仅项目负责人可提交，必须已选择指导老师
- 提交后状态: `draft` → `wait_teacher_audit`
- 自动发送消息通知指导老师

#### 3.4.4 导师审核

- 路径: `POST /projects/teacher-audit`
- 权限: 仅该项目的指导老师可审核

**请求体**:

```json
{
  "projectId": 1001,
  "result": "pass",
  "opinion": "项目可行，同意申报"
}
```

- 通过后状态: `wait_teacher_audit` → `wait_college_assign`
- 驳回后状态: `wait_teacher_audit` → `rejected`
- 自动发送消息通知项目负责人

#### 3.4.5 学院终审

- 路径: `POST /projects/college-audit`
- 通过后状态: `wait_college_audit` → `wait_school_assign`
- 驳回后状态: `wait_college_audit` → `rejected`

#### 3.4.6 学校终审

- 路径: `POST /projects/school-audit`
- 通过后状态: `wait_school_audit` → `approved`
- 驳回后状态: `wait_school_audit` → `rejected`

### 3.5 评审管理接口 (`/api/v1/reviews`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/reviews/scores` | 提交评分 | expert, teacher, college_admin, school_admin |
| GET | `/reviews/scores` | 获取评审打分列表 | 已登录 |
| POST | `/reviews/assignments` | 分配专家 | college_admin, school_admin |
| GET | `/reviews/assignments` | 获取专家分配列表 | 已登录 |
| GET | `/reviews/my-tasks` | 获取我的待评审任务 | expert, teacher, college_admin, school_admin |
| GET | `/reviews/pending-projects` | 获取待分配项目列表 | college_admin, school_admin |
| GET | `/reviews/overview` | 评审概览统计 | college_admin, school_admin |

#### 3.5.1 提交评分

**请求体**:

```json
{
  "projectId": 1001,
  "stage": "college",
  "scoreInnovation": 22,
  "scoreFeasibility": 20,
  "scoreTeam": 18,
  "scoreValue": 20,
  "totalScore": 80
}
```

**响应体**:

```json
{
  "code": 200,
  "message": "评分成功",
  "data": {
    "scoreId": 5001
  }
}
```

> 校验逻辑：
> 1. 通过 `userId` 获取或自动创建 `Expert` 记录
> 2. 校验该专家已被分配此项目此阶段的评审任务
> 3. 校验未重复打分
> 4. 打分后自动检查是否所有分配专家均已完成，若是则自动推进项目状态

#### 3.5.2 分配专家

**请求体**:

```json
{
  "projectId": 1001,
  "expertId": 4,
  "stage": "college",
  "deadline": "2024-05-30T23:59:59"
}
```

> 分配后自动推进项目状态：`wait_college_assign` → `wait_college_review` 或 `wait_school_assign` → `wait_school_review`。同时发送消息通知有关联 userId 的专家。

#### 3.5.3 获取我的待评审任务

返回当前用户的所有待处理任务，包括：
1. **评审打分任务**（来自 `expert_assignment`），附带是否已打分标记
2. **审核任务**（根据角色自动匹配）：
   - teacher: `wait_teacher_audit` 状态的自己的项目
   - college_admin: 本院 `wait_college_audit` 状态的项目
   - school_admin: 所有 `wait_school_audit` 状态的项目

### 3.6 专家管理接口 (`/api/v1/experts`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/experts` | 获取专家列表 | 已登录 |
| POST | `/experts` | 创建专家 | college_admin, school_admin |
| PUT | `/experts/{id}` | 更新专家 | college_admin, school_admin |
| DELETE | `/experts/{id}` | 删除专家 | school_admin |

### 3.7 成果管理接口 (`/api/v1/achievements`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/achievements` | 提交成果 | student, teacher |
| GET | `/achievements` | 获取成果列表 | 已登录 |
| GET | `/achievements/{id}` | 获取成果详情 | 已登录 |
| PUT | `/achievements/{id}` | 更新成果 | student, teacher, college_admin, school_admin |
| DELETE | `/achievements/{id}` | 删除成果 | college_admin, school_admin |

### 3.8 里程碑管理接口 (`/api/v1/milestones`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/milestones` | 获取里程碑列表 | 已登录 |
| POST | `/milestones` | 创建里程碑 | student, teacher, college_admin, school_admin |
| PUT | `/milestones/{id}` | 更新里程碑 | student, teacher, college_admin, school_admin |
| DELETE | `/milestones/{id}` | 删除里程碑 | college_admin, school_admin |

### 3.9 消息中心接口 (`/api/v1/messages`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/messages` | 获取消息列表(分页+筛选) | 已登录 |
| GET | `/messages/unread-count` | 获取未读消息数量 | 已登录 |
| PUT | `/messages/{id}/read` | 标记单条消息已读 | 已登录 |
| PUT | `/messages/read-all` | 全部标记已读 | 已登录 |
| DELETE | `/messages/{id}` | 删除消息 | 已登录 |
| POST | `/messages/send` | 发送消息(管理员) | college_admin, school_admin |

### 3.10 文件上传接口 (`/api/v1/files`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/files/upload` | 上传文件(multipart/form-data) | 已登录 |
| GET | `/files/{id}/download` | 获取文件下载链接(30分钟有效) | 已登录 |

#### 3.10.1 上传文件

- 路径: `POST /files/upload`
- Content-Type: `multipart/form-data`
- 参数:
  - `file`: 上传的文件（MultipartFile）
  - `type`: 附件类型（`apply`/`mid`/`conclude`/`achievement`）
  - `relationId`: 关联业务ID（默认0）

**响应体**:

```json
{
  "code": 200,
  "data": {
    "attachId": 1,
    "attachType": "apply",
    "relationId": 1001,
    "fileName": "申报书.pdf",
    "fileSize": 1048576,
    "minioPath": "apply/1001/xxx.pdf",
    "uploadUser": 1,
    "uploadTime": "2024-04-19T12:00:00"
  }
}
```

### 3.11 学院管理接口 (`/api/v1/colleges`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/colleges` | 获取学院列表 | 已登录 |
| POST | `/colleges` | 创建学院 | school_admin |
| PUT | `/colleges/{id}` | 更新学院 | school_admin |
| DELETE | `/colleges/{id}` | 删除学院 | school_admin |

### 3.12 项目类别管理接口 (`/api/v1/categories`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/categories` | 获取类别列表 | 已登录 |
| POST | `/categories` | 创建类别 | school_admin |
| PUT | `/categories/{id}` | 更新类别 | school_admin |
| DELETE | `/categories/{id}` | 删除类别 | school_admin |

### 3.13 数据统计接口 (`/api/v1/statistics`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/statistics/overview` | 总览数据 | 已登录 |
| GET | `/statistics/by-category` | 按类别统计 | 已登录 |
| GET | `/statistics/by-year` | 按年份统计 | college_admin, school_admin |
| GET | `/statistics/by-college` | 按学院统计 | college_admin, school_admin |

---

## 4. 已知设计问题与改进计划

| 问题 | 影响 | 改进建议 |
|------|------|----------|
| 校外专家 `userId` 为 NULL，无法登录系统进行评审打分 | 外部专家无法参与线上评审，只能分配校内专家 | 为外部专家创建临时登录账号，或将 expert 表与 user 表强关联 |
| 缺少删除专家分配的 API | 误分配专家后无法撤销 | 增加 `DELETE /reviews/assignments/{id}` 接口 |
| `PUT /projects/{id}` 忽略 status 字段 | 无法通过通用更新接口修改状态（为设计如此，防止绕过审核流程） | 如需修改状态，必须通过专用审核接口 |
| 项目成员数量无限制 | 理论上可添加无限成员 | 增加 PRD 要求的"同一学生同期最多参与2项"校验 |

---

## 5. 前端页面结构

| 页面路径 | 功能 | 对应角色 |
|----------|------|----------|
| `/login` | 登录 | 所有 |
| `/register` | 注册 | 所有 |
| `/dashboard` | 仪表盘 | 所有 |
| `/profile` | 个人信息 | 所有 |
| `/project/*` | 项目管理（创建/列表/详情/审核） | 所有 |
| `/review/*` | 评审管理（分配/打分/任务） | 管理员/专家 |
| `/expert/*` | 专家管理 | 管理员 |
| `/achievement/*` | 成果管理 | 学生/教师 |
| `/milestone/*` | 里程碑管理 | 学生/教师 |
| `/message/*` | 消息中心 | 所有 |
| `/statistics/*` | 数据统计 | 管理员 |
| `/college/*` | 学院管理 | 管理员 |
| `/category/*` | 类别管理 | 管理员 |
| `/user/*` | 用户管理 | 管理员 |
